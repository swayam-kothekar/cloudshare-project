{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/share/:path*", "destination": "https://cloudshare-project.vercel.app/share/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}